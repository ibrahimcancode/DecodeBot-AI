# Chatbot Engine dependencies (Week 1) — stdlib only; none required.

# Machine Learning Engine dependencies (Week 2) — not required by the Chatbot Engine (FR-230).
scikit-learn>=1.3
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
joblib>=1.3

# Web Demo (Streamlit) — also used by Streamlit Community Cloud
streamlit>=1.28.0

# OCR Engine (optional locally; declared here so the Streamlit Cloud web demo
# can exercise the Recognition tab). Local CLI/GUI users may instead install
# only via: pip install -r requirements-ocr.txt
# The Tesseract system binary is still required (see packages.txt for Cloud).
opencv-python-headless>=4.8
pytesseract>=0.3.10
